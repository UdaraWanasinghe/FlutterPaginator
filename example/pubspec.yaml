name: flutter_paginator_example
description: flutter_paginator example application.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_paginator:
    path: ../

  http: ^0.13.4

flutter:
  uses-material-design: true